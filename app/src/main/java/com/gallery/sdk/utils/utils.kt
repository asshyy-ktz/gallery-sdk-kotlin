package com.gallery.sdk.utils

import android.os.Build
import android.os.Environment

class utils {

//    protected val outputDirectory: String by lazy {
//        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q) {
//            "${Environment.DIRECTORY_DCIM}/GallerySDK/"
//        } else {
//            "${applicationContext.getExternalFilesDir(Environment.DIRECTORY_DCIM)}/GallerySDK/"
//        }
//    }
}